# Copy to .env.local for local development. On Vercel, set in Project → Settings → Environment Variables.
OPENAI_API_KEY=
# Optional override (defaults to gpt-4o-mini in lib/planner.ts)
OPENAI_MODEL=gpt-4o-mini
